jdk:
  - openjdk17

# Sprint 3C — JitPack build config. Consumers add:
#   maven { url = uri("https://jitpack.io") }
# to their repositories, then:
#   implementation("com.github.MarvelTechnologies:Attribr-by-MRVL:1.3.0")
# (or whichever tag / branch / commit hash they want to pin to).
before_install:
  - echo "Using JDK 17"

install:
  - ./gradlew :attribr:publishToMavenLocal
